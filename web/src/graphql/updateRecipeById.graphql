mutation UpdateRecipeById($input: EditRecipeInput!) {
    updateRecipeById(input: $input) {
        id
        email
        recipes {
            id
            title
            image
            mealType
        }
    }
}