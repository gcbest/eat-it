import React from 'react'
import { CartItem } from 'lib/interfaces'

const CartItem: React.FC<CartItem> = ({name, aisle, isChecked}) => {
    return (
        <div>
            
        </div>
    )
}

export default CartItem
